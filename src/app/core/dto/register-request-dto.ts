export interface RegisterRequestDto {
  cardId: string;
  fullName: string;
  email: string;
  numberCellphone: number;
  // v Automatically added from the backend v
  // active: boolean;
  // password: string;
  // rol: string;
}
